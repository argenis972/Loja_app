# Copiar a .env y editar valores reales para Postgres
DB_USER=loja_user
DB_PASSWORD=changeme
DB_HOST=localhost
DB_PORT=5432
DB_NAME=loja_db
DATABASE_URL=postgresql+psycopg://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
STORAGE_BACKEND=postgres
