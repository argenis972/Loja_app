name: PR Checks

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  validate-pr:
    name: Validar Pull Request
    runs-on: ubuntu-latest    
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4
      
      - name: Verificar tamanho da PR
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;
            
            console.log(`📊 Estatísticas da PR:`);
            console.log(`   Arquivos alterados: ${pr.changed_files}`);
            console.log(`   Linhas adicionadas: ${pr.additions}`);
            console.log(`   Linhas removidas: ${pr.deletions}`);
            
            const totalChanges = pr.additions + pr.deletions;
            
            if (pr.changed_files > 30) {
              core.warning(
                `⚠️ Esta PR altera ${pr.changed_files} arquivos. ` +
                `PRs grandes são mais difíceis de revisar. ` +
                `Considere dividir em PRs menores.`
              );
            }
            
            if (totalChanges > 1000) {
              core.warning(
                `⚠️ Esta PR tem ${totalChanges} linhas de mudança. ` +
                `PRs menores facilitam o code review.`
              );
            } else {
              core.info(`✅ Tamanho da PR está adequado`);
            }
      
      - name: Verificar descrição da PR
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;
            const body = pr.body || '';
            
            if (body.length < 30) {
              core.warning(
                '⚠️ Descrição da PR muito curta! ' +
                'Por favor, explique:\n' +
                '  - O que foi alterado\n' +
                '  - Por que foi alterado\n' +
                '  - Como testar'
              );
            } else {
              core.info('✅ Descrição da PR adequada');
            }
      
      - name: Verificar se PR tem label
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;
            
            if (pr.labels.length === 0) {
              core.notice(
                '💡 Dica: Adicione labels à PR (feat, fix, docs, etc.) ' +
                'para facilitar organização'
              );
            }

  summary:
    name: Resumo das Verificações
    runs-on: ubuntu-latest
    needs: [validate-pr]
    if: always()
    
    steps:
      - name: Resumo
        run: |
          echo "✅ Verificações de PR concluídas"
          echo "👀 Aguardando code review..."