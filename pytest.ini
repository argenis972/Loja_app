[pytest]
pythonpath = .
testpaths =
	backend/tests
	tests
asyncio_mode = auto
addopts = --strict-markers --maxfail=1 -q --cov=backend --cov-report=term-missing

[pytest]
testpaths = backend/tests
python_files = test_*.py
addopts = -q
